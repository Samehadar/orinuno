<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.orinuno</groupId>
        <artifactId>orinuno-parent</artifactId>
        <version>0.1.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>kodik-sdk</artifactId>
    <packaging>jar</packaging>

    <name>kodik-sdk</name>
    <description>
        Standalone Kodik SDK: HTTP client (KodikApiClient + rate limiter + response mapper),
        token management (KodikTokenRegistry + auto-discovery + validator), decoder pipeline
        (KodikDecoder with the ROT13+Base64 8-step path), embed shortcut (KodikEmbedClient).

        ADR 0018 Phase 1 — symmetric to jutsu-sdk. Spring-WebFlux is the only non-trivial
        transitive dependency (WebClient is the HTTP layer); no Spring Boot, no
        auto-configuration, no @Component/@Service annotations. Consumers wire SDK beans
        manually — orinuno-app reaches for kodik-sdk-spring-boot-starter to do that.

        Phase 1.8 will absorb kodik-sdk-drift into this module under com.kodik.drift so the
        Kodik SDK ships as a single artefact.
    </description>

    <dependencies>
        <!--
            HTTP client + reactive primitives. spring-context is needed because WebClient.Builder
            transitively reaches SmartLifecycle when wiring its default exchange function on
            Spring 6.x; explicitly declared here so the SDK is usable without a Spring Boot
            starter on the classpath. Symmetric to jutsu-sdk.
        -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-webflux</artifactId>
            <version>${spring-webflux.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
            <version>${spring-webflux.version}</version>
        </dependency>
        <dependency>
            <groupId>io.projectreactor.netty</groupId>
            <artifactId>reactor-netty-http</artifactId>
            <version>${reactor-netty.version}</version>
        </dependency>

        <!--
            Raw-response-first mapping + drift detection. The SDK serialises POST form bodies
            and parses Kodik's JSON envelopes via Jackson; ObjectMapper.convertValue powers the
            schema-drift detector (today still in kodik-sdk-drift; absorbed in Phase 1.8).
        -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Outbound RPS budget — symmetric to jutsu-sdk's rate limiter. -->
        <dependency>
            <groupId>com.bucket4j</groupId>
            <artifactId>bucket4j_jdk17-core</artifactId>
            <version>${bucket4j.version}</version>
        </dependency>

        <!-- Optional metrics: SDK uses MeterRegistry if provided, else falls back to a no-op. -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
            <version>${micrometer.version}</version>
        </dependency>

        <!-- @Nullable for nullable-by-design parameters. -->
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>${jakarta-annotation.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-test</artifactId>
            <version>${reactor.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>spotless-check</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
