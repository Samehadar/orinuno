<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.14</version>
        <relativePath/>
    </parent>

    <groupId>com.orinuno</groupId>
    <artifactId>orinuno-source-jutsu</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>orinuno-source-jutsu</name>
    <description>
        Standalone Spring Boot deployable: per-source service for jut.su (ADR 0019).

        Consumes jutsu-sdk for the parsing surface (DLE auth, sticky cookies, 1 RPS
        rate-limit, premium decode), owns the jutsu_* MySQL schema, and serves the
        historical /api/v1/sources/jutsu/*, /api/v1/sources/jutsu/stream,
        /api/v1/providers/jutsu/stream endpoints plus the canonical
        /api/v1/source-events/ready event stream.

        The Playwright live-fallback (JutsuLiveFallbackService + circuit breaker +
        negative cache) co-locates with the rest of the L1 code here so a Chromium
        OOM blast stays bounded to this service's JVM, never propagates to the
        orinuno gateway or the Kodik path (ADR 0019 §"Playwright live-fallback").

        Per ADR 0019 §"Reactor changes", no jutsu-sdk-spring-boot-starter exists
        today — inline @Configuration is enough until a second jut.su host
        materialises.
    </description>

    <properties>
        <java.version>21</java.version>
        <mybatis-spring-boot-starter.version>3.0.5</mybatis-spring-boot-starter.version>
        <springdoc-openapi.version>2.8.17</springdoc-openapi.version>
    </properties>

    <dependencies>
        <!--
            jutsu-sdk — already extracted as a standalone Maven artefact via ADR 0012.
            Carries the rate limiter, session manager, decoder, source parser, plus
            ADR 0015's full-browser-parity surface (catalog, search, anime info,
            episode meta, notice feed, drift detection).
        -->
        <dependency>
            <groupId>com.orinuno</groupId>
            <artifactId>jutsu-sdk</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!--
            Sealed SourceCatalogEvent contract for /api/v1/source-events/ready.
            Identical shape to what orinuno-source-kodik emits; consumers (meter,
            Kin downstream jut.su consumer) require no change to subscribe.
        -->
        <dependency>
            <groupId>com.orinuno</groupId>
            <artifactId>orinuno-source-contract</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>
        <!--
            JDBC + MySQL driver + Liquibase. Phase 4.2 will move the jutsu_*
            changelogs from orinuno-app into this module so this service becomes
            the single owner of the jutsu_* tables in its dedicated schema.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis-spring-boot-starter.version}</version>
        </dependency>
        <!-- OpenAPI annotations on DTOs + Swagger UI surface. -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webflux-ui</artifactId>
            <version>${springdoc-openapi.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Testcontainers MySQL — Phase 4.2 smoke test runs Liquibase against
             a real MySQL 8.0 container so the jutsu_* changelog can't drift. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>mysql</artifactId>
            <version>1.21.4</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>1.21.4</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>orinuno-source-jutsu</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Include *IT classes alongside *Test/*Tests; e2e-tagged
                         tests (Testcontainers, slow) are opt-in via -Dgroups=e2e.
                         Default `mvn test` skips them via excludedGroups=e2e. -->
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*IT.java</include>
                    </includes>
                    <excludedGroups>e2e</excludedGroups>
                </configuration>
            </plugin>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>2.46.1</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <googleJavaFormat>
                            <version>1.22.0</version>
                            <style>AOSP</style>
                            <reflowLongStrings>true</reflowLongStrings>
                        </googleJavaFormat>
                        <removeUnusedImports/>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <id>spotless-check</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            E2E tests (Testcontainers MySQL, Liquibase apply, slow).
            Activate with:  mvn -Pe2e test
            Mirrors orinuno-app + meter profile of the same id.
        -->
        <profile>
            <id>e2e</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <includes>
                                <include>**/*Test.java</include>
                                <include>**/*Tests.java</include>
                                <include>**/*IT.java</include>
                            </includes>
                            <excludedGroups combine.self="override"/>
                            <groups>e2e</groups>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
